# Fine-tune (QLoRA / LLaMA-Factory) — instale DEPOIS de requirements.txt:
#   pip install -r requirements.txt
#   pip install -r requirements-sft.txt
#
# Requer Python 3.11 ou 3.12 (não use 3.13/3.14 — torch/llamafactory quebram o resolver).
# GPU NVIDIA: se torch sem CUDA, reinstale de https://pytorch.org antes do finetune.
#
# llamafactory puxa torch, transformers, peft, accelerate, trl, datasets etc.
# Não liste esses pacotes aqui com >= solto — pip entra em resolution-too-deep.
llamafactory==0.9.5
bitsandbytes>=0.45.0
