# Fine-tune — Unsloth (bf16 LoRA).
#
#   pip install -r requirements.txt
#   pip install -r requirements-sft.txt
#
# Requer Python 3.11 ou 3.12 e GPU NVIDIA (torch CUDA).
# Unsloth puxa transformers v5+, trl, peft, etc.
#
# Qwen3.5: use bf16 LoRA (load_in_16bit); QLoRA 4-bit não é recomendado pela Unsloth.
unsloth
unsloth_zoo
bitsandbytes>=0.45.0
datasets>=2.20
